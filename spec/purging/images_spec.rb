require "spec_helper"

describe Purging::Images do
  it "has a version number" do
    expect(Purging::Images::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
